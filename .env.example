# Valores de DEV usados pelo docker-compose.yml da raiz. NAO use em producao.
# Copie para .env (que e ignorado pelo git) e ajuste se quiser.
API_KEY=chave-dev
ADMIN_KEY=admin-dev
PAINEL_SENHA=painel-dev
